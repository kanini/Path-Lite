# screens/

Container components representing full screens in the application.

**Naming convention:** PascalCase with "Screen" suffix (e.g., `LoginScreen.tsx`)

**Guidelines:** One screen per file. Screens should use navigation props and services for data fetching.
