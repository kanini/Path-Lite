# navigation/

Navigation configuration and routing logic for the application.

**Naming convention:** PascalCase for navigators (e.g., `RootNavigator.tsx`)

**Guidelines:** Keep navigation logic separate from screens. Define all route types in `types.ts`.
