# types/

Shared TypeScript type definitions, interfaces, and enums used across the application.

**Naming convention:** PascalCase for types and interfaces (e.g., `User.ts`, `ApiResponse.ts`)

**Guidelines:** Only shared types live here. Feature-specific types should stay co-located with the feature.
