# components/

Reusable, presentational UI components shared across screens.

**Naming convention:** PascalCase (e.g., `Button.tsx`, `Card.tsx`)

**Guidelines:** Components should be pure, testable, and reusable. Avoid business logic inside components.
