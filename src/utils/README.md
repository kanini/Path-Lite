# utils/

Pure utility functions and helpers used across the application.

**Naming convention:** camelCase (e.g., `formatDate.ts`, `validation.ts`)

**Guidelines:** Functions should be pure, testable, and reusable with no side effects.
