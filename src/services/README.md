# services/

API clients, data fetching, and business logic services.

**Naming convention:** camelCase with "Service" suffix (e.g., `authService.ts`)

**Guidelines:** Services should be stateless, testable, and follow the single responsibility principle.
